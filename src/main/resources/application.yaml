---
spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://gz-cynosdbmysql-grp-17pikoep.sql.tencentcdb.com:22639/pancake_job
    username: HELLO
    password: JBGZ
  config:
    activate:
      on-profile: dev
server:
  port: 8080

---
spring:
  config:
    activate:
      on-profile: test
  application:
    name: pankcake_job1
  freemarker:
    template-loader-path: classpath:/web/app/
    suffix: .ftl
    charset: utf-8
    cache: false
    expose-request-attributes: true
    expose-session-attributes: true
  web:
    resources:
      static-locations: classpath:/static/
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    name: defaultDataSource
    url: jdbc:mysql://gz-cynosdbmysql-grp-17pikoep.sql.tencentcdb.com:22639/pancake_job?serverTimezone=UTC
    username: HELLO
    password: JBGZ
server:
  port: 5000

mybatis:
  mapper-locations: classpath:mapper/*.xml  # 扫描所有mybatis的xml文件，兼容plus
#  configuration:
#    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl   # mysql的日志输出
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl   #由plus管理
---
spring:
  profiles:
    active: test